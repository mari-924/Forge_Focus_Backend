package com.focusforge.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class DataSourceConfig {
    // Remove custom DataSource bean — let Spring Boot auto-configure it
}